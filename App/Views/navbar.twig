<nav class="navbar navbar-expand-lg border-bottom mb-1">
    <div class="container">
        <a class="navbar-brand" href="/">IRP</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% if current_user %}
                    <li class="nav-item">
                        <a class="nav-link" href="/profile/show">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/messages/index">Inbox</a>
                    </li>
                    {% if current_user.type == 'admin' %}
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/">Admin Dashboard</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup/new">Sign up</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
